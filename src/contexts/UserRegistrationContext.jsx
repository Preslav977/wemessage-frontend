import { createContext } from "react";

export const UserSignUpObjectContext = createContext(null);

export const FirstNameContext = createContext(null);

export const LastNameContext = createContext(null);

export const UserNameContext = createContext(null);

export const PasswordContext = createContext(null);

export const ConfirmPasswordContext = createContext(null);

export const BioContext = createContext(null);

export const ProfilePictureContext = createContext(null);

export const BackgroundPictureContext = createContext(null);
