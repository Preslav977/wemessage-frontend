import { createContext } from "react";

export const UserLogInObjectContext = createContext(null);

export const UserLoggedInContext = createContext(null);

export const UserLoggedInGetByIdContext = createContext(null);
