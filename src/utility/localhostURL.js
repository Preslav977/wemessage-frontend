//this will replace all currently used localhost URLs in order to make it easier to swap later
// const localhostURL = "http://localhost:5000";

const localhostURL =
  "wemessage-frontend-eq1v0vrsw-preslav977s-projects.vercel.app";

export default localhostURL;
